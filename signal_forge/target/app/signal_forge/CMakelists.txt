find_package(Threads REQUIRED)

add_compile_definitions(_GNU_SOURCE)

add_executable(signal_forge
    src/main.c
    src/project.c
    src/sf_target.c
    src/supervisor.c
    src/sf_receiver.c
)

target_include_directories(signal_forge
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_link_libraries(signal_forge
PRIVATE
    signal_forge_target
    task_host_core
    dbg_pubsub
    pthread
    rt
)