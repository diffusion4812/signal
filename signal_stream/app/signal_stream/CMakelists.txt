add_executable(signal_stream WIN32
    src/main.cpp
    src/line_plot.cpp
    src/ss_plot.cpp
    src/project_serialization.cpp
    src/source_config.cpp
)

target_include_directories(signal_stream
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_link_libraries(signal_stream
    PRIVATE
        SignalCore::ui
        SignalStream::App
        SignalStream::Core
        SignalStream::ApacheBridge
        SignalStream::AdapterRandom
        SignalStream::AdapterMQTT
        SignalStream::AdapterSignalForge
        SignalStream::AdapterOPCUA
        Arrow::arrow_shared
        libglew_static
        Freetype::Freetype
        spdlog::spdlog
        Boost::headers
        Boost::json
        wx::wxcore
        wx::wxaui
        wx::wxgl
        wx::propgrid
)